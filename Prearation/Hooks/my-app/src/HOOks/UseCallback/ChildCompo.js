import React from 'react'

const ChildCompo = ({HandleClick}) => {
  return (
    <div>
        <button onClick={HandleClick}>Click me</button>
      
    </div>
  )
}

export default ChildCompo
